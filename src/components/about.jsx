import React from "react";
import PhotoCircular from "./photo-circular/photo-circular";
const About = (props) => {
  return (
    <div>
      <PhotoCircular></PhotoCircular>
    </div>
  );
};

export default About;
